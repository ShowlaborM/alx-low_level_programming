#include "main.h"

/**
 * _islower - check for lowercase character
 * @c: the char to check from
 * Return: 1 if c is lowercase, 0 otherwise
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
